# GitHub Action to create a PDF and commit it to the repository
name: Markdown to PDF GitHub Action

# Run this workflow every time a change is pushed to the 'main' branch
on:
  push:
    branches:
      - main

jobs:
  build_and_commit_CV_in_pdf:
    runs-on: ubuntu-latest

    # Grant permissions for the action to write to the repository
    permissions:
      contents: write

    steps:
      # Step 1: Check out your repository code
      - name: Check out repository
        uses: actions/checkout@v4

      # Step 2: Install Pandoc and the necessary LaTeX packages
      - name: Install Pandoc and LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-latex-base texlive-fonts-recommended texlive-latex-extra

      # Step 3: Run the Pandoc command to convert the file
      - name: Convert Markdown to PDF
        run: |
          pandoc michael_forbes_cv.md \
            --from=markdown \
            --to=pdf \
            --output=Michael_Forbes_CV.pdf \
            --variable=geometry:margin=1in

      # Step 4: Configure Git for the commit
      - name: Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      # Step 5: Commit, Rebase, and Push the generated PDF
      - name: Commit and Push PDF
        run: |
          git add Michael_Forbes_CV.pdf
          # Use diff-index to check if there are changes to commit
          if ! git diff-index --quiet --cached HEAD; then
            git commit -m "Automated build of CV in PDF"
            # Use pull with rebase to avoid merge conflicts
            git pull --rebase
            git push
          else
            echo "No changes to commit."
          fi
          
      # Step 6 (Optional): Upload the PDF as a downloadable artifact
      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: CV-PDF
          path: Michael_Forbes_CV.pdf
